# ほげ走

ブラウザで遊べる、紅茶ファンタジー風の横スクロール・ローグライクランゲームです。

## 主な機能

- ユーザー名だけで開始（Supabase Anonymous Sign-In）
- PC / スマホ対応
- PC：ジャンプ `Space / W / ↑`、しゃがみ `S / ↓`
- スマホ：上フリックでジャンプ、下フリックでしゃがみ
- スタート前にキーバインド変更可能
- 紅茶カップを集めてランダム3択の強化
- プレイヤーごとの自己ベスト TOP5 ランキング
- 走行・ジャンプ・着地・しゃがみの専用アニメーション
- うさぎ・紅茶モチーフのHUD / 能力選択UI
- GitHub Pagesでそのまま公開可能

## Supabase設定

1. SupabaseでProjectを作成します。
2. Dashboard の **SQL Editor** で `supabase.sql` を実行します。
3. Authentication の **Anonymous Sign-Ins** を有効にします。
4. Project URL と Publishable key を確認します。
5. `config.js` を書き換えます。

```js
window.HOGE_CONFIG = {
  supabaseUrl: "https://xxxxxxxx.supabase.co",
  supabaseKey: "sb_publishable_xxxxxxxxxxxxx"
};
```

ブラウザ側には **service_role key を置かないでください**。

## GitHub Pagesへのアップロード

1. このフォルダをZIPから解凍します。
2. GitHubのリポジトリで **Add file → Upload files** を選びます。
3. `index.html`、`styles.css`、`game.js`、`config.js`、`supabase.sql`、`README.md`、`assets/` をアップロードします。
4. **Settings → Pages** で `main` / `/(root)` を公開対象にします。

## 現在の画像ファイル構成

```text
assets/
├── backgrounds/
│   ├── stage-bg.png
│   └── ability-select-bg.png
├── ui/
│   ├── title-logo.png
│   ├── title-key-art.png
│   └── hud/
│       ├── score.png
│       ├── tea-cup.png
│       ├── next-item.png
│       ├── best.png
│       └── build.png
├── player/
│   ├── portrait.jpg
│   ├── idle.png
│   ├── gameover.png
│   ├── run/
│   │   └── run-01.png ～ run-16.png
│   ├── jump/
│   │   ├── up.png
│   │   ├── apex.png
│   │   └── down.png
│   ├── landing/
│   │   └── land-1.png ～ land-3.png
│   └── slide/
│       └── slide-1.png ～ slide-3.png
├── enemies/
│   └── enemy-sheet.png
├── items/
│   └── tea-cup.png
└── upgrades/
    ├── double-jump.png
    ├── jump-boots.png
    ├── shield.png
    ├── magnet.png
    ├── slow-clock.png
    ├── tiny-charm.png
    ├── revive.png
    └── tea-sensor.png
```

画像はすべて `assets/` 以下へ用途別に整理済みです。ルート直下の重複画像や、現在のコードから使用されていない旧素材は削除しています。


## v21 画像パス修正 / GitHub Pages キャッシュ対策

画像フォルダ構成は以下に統一しています。

```text
assets/
├─ backgrounds/
├─ enemies/
├─ items/
├─ player/
├─ ui/
│  └─ hud/
└─ upgrades/
```

特にタイトル画像は `assets/ui/title-key-art.png`、タイトルロゴは `assets/ui/title-logo.png` を参照します。
`index.html` / `styles.css` / `game.js` の画像参照をこの階層へ統一し、GitHub Pagesで旧ファイルがキャッシュされないよう `?v=21` を付与しています。


## v23 ローディング画面レイアウト修正

- ローディング画面のガイドカードが崩れないように幅・文字サイズ・カード配置を調整。
- Now Loading 右側のキャラ表示をやめ、`assets/items/tea-cup.png` を使ったゆらゆら揺れるティーカップ演出に変更。
- CSS / JS のアセット参照バージョンを更新。


## v24 HUD簡略化とランキング位置修正
- 上部HUDの個別カード枠・うさぎ耳フレームを撤去し、半透明の横長バーに簡略化。
- 各ステータスは大きめのアイコンを右、数値を左に置くレイアウトへ変更。
- スタート画面内のジャンプ/しゃがみ説明POPを非表示化。
- `.game-column` の閉じタグ不足を修正し、ランキングパネルがPCでゲーム画面右側に並ぶよう修正。


## v25 能力選択UI全面整理
- 能力選択画面をゲーム画面の16:9内に完全固定し、縦横スクロールを撤廃。
- 3つの能力カードを常時1画面に表示し、アイコン・能力名・Lv・説明文の順で整列。
- カード内の重複した「この力を選ぶ」ボタンを削除し、カード全体をクリック対象に変更。
- 選択中のみチェックマークと発光枠を表示し、未選択は軽くトーンダウン。
- 最終決定ボタンを画面下中央に1つだけ配置し、比較しやすいUIへ整理。


## v26 能力選択後の安全時間

- 能力を選択した瞬間、画面内に存在している敵をすべて削除します。
- 能力選択画面を閉じてゲームに戻ってから **1秒間**、新しい敵と紅茶カップを出現させません。
- 既に画面内に存在している紅茶カップは残します。
- 安全時間中は敵・紅茶カップのスポーンタイマーも停止するため、安全時間終了直後に一斉出現しにくい仕様です。


## v27 リザルト画面スコア強調
- GAME OVER時に専用のRESULT SCORE表示を追加。
- 最終スコアをリザルト内で最も大きく中央表示。
- TEA数、失敗理由、ランキング保存状況は補助情報として小さく整理。


## v28 キーボード操作・1画面メニュー/リザルト

- スタート / リトライを Space キーでも決定可能にしました。
- 能力選択を左から Q/W/E または 1/2/3 で直接決定できるようにしました。テンキー1/2/3も同じ扱いです。
- 能力選択キーもメニュー画面の KEY CONFIG から変更できます。既存の W ジャンプと W 中央能力のような、ゲーム中/メニュー中で用途が違う重複キーは許可しています。
- スタート画面は半透明POPをやめ、背景を隠す1画面メニューに変更。不要な操作説明カードを外し、キー設定を画面内に統合しました。
- リザルト画面も1画面固定にしてスクロールを廃止し、最終スコアを最優先表示にしています。
- 能力カードには現在のショートカットキーを表示します。


## v29 強化表示と復活羽の残機表示
- ゲーム画面左上のジャンプ/しゃがみ説明POPを削除。
- 「今回の強化」を横チップ型から、アイコン＋能力名の縦並びPOPに変更。
- 復活の羽を所持している間は、能力名の右側に羽アイコンを残数分表示。使用時は1枚ずつ減少。


## v30 調整内容
- ロード画面の「ほげ走」ロゴ / Now Loading / ティーカップを中央配置に変更し、操作説明を削除。
- 能力選択中は左上の強化POPを自動で隠し、表示の被りを防止。
- お化け敵は通常ジャンプ1回では回避しにくい高さへ調整。ジャンプ系強化取得で突破しやすく変更。
- 10000m以降はジャンプする野菜、30000m以降は高速野菜、50000m/70000m以降はそれぞれ出現頻度アップ。
- 50000m以降は2段目の床を追加。ジャンプで上段、しゃがみで下段へ移動可能。


## v31 調整内容
- HUDの各ステータスを「アイコン→数値」の順に修正。
- アイコンと数値の距離を近く、各ステータス間は少し広めに調整。
- 「今回の強化」エリアの大枠を削除し、より小さくコンパクトな表示に変更。


## v32 調整内容
- ロード画面のタイトルロゴ周囲の四角フレームを削除。
- 強化が未取得のときは「今回の強化」エリアを非表示化。
- 強化取得後も見出し文言「今回の強化」を表示しないよう変更。


## v33 調整内容
- HUD内の薄い内側の四角 / 区切り線を削除。
- 外側の丸い枠だけを残し、内部はシンプル表示に調整。


## v34 調整内容
- 名前欄付近に残っていたキーバインド説明系の表示を削除。
- スタート画面の決定/能力選択キー説明文を非表示化。

## v35 起動時のキーバインド表示チラつき修正・favicon追加
- CSS読込前にスタート画面が一瞬描画されるFOUC対策として、head内に最小限の起動用CSSを追加。
- `body.app-loading` 中は `.app-shell` を即時非表示にし、ロード完了までメニューやキーバインド説明が見えないよう修正。
- 添付画像を `assets/ui/favicon.png` と `assets/ui/apple-touch-icon.png` に設定。


## v36 HOME導線・名前変更位置・ゲーム内キー説明修正
- リザルト画面に HOME ボタンを追加し、HOME画面へ戻れるように変更。
- 名前変更ボタンをゲームHUDから削除し、HOME画面内へ移動。
- HOME画面のキーバインド設定・説明は維持。
- ゲーム開始直後に名前の裏へ数秒表示されていた `JUMP ↑ / SPACE   SLIDE ↓` のCanvas描画を完全削除。


## v39 タイトル位置・ランキング装飾
- PC表示で最上部タイトルロゴの横位置をゲーム画面列の中心に厳密に合わせた。
- ランキング枠に生成済みの1/2/3位メダル、王冠バッジ、コーナー飾り、仕切り装飾を適用。
- 上位3件に金・銀・銅の背景ニュアンスを追加。


## v40 ランキング枠調整
- PC表示時のランキング枠の高さをゲーム画面の高さに自動同期。
- `assets/ui/ranking/header-badge.png` をランキング枠の上辺に重ねて配置。
- 新しいランキングロゴ `assets/ui/ranking/ranking-logo.png` を追加して反映。
- `ONLINE` と「プレイヤーごとの自己ベストを表示」の文章を削除。


## v41 ランキング上部レイアウト調整
- header-badge の上端をランキング枠の上辺に合わせて配置。
- ランキングロゴとウサギ装飾を上へ詰め、TOP5領域の余白を削減。
- ランキングロゴを背景透過版へ差し替え。
- 長いプレイヤー名は省略せず、必要に応じて折り返して全文表示。


## v45 キャラクター完全差し替え
- ゲーム内プレイヤーの待機・走行16コマ・ジャンプ3種・着地3コマ・スライド3コマ・ゲームオーバーを新キャラへ置換。
- スタート/メニュー等で使う `assets/ui/title-key-art.png` も新キャラへ置換。
- `assets/player/portrait.jpg` も新キャラへ置換。
- キャッシュ対策として参照バージョンを v45 に更新。
